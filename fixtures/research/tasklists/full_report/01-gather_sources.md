---
id: gather_sources
output:
  sources: array
  query: string
---

Search for relevant sources on the research topic. Use searchWeb() to find at least 3 sources. Save the search results as notes using saveNote(). Return the list of source URLs and the query used.
