---
id: search
output:
  search_results: array
  query: string
---

Use searchGoogle() to find relevant pages for the `query` variable (from seed context). Return the top 5 search results as an array and the query used.
