{
  "extends": "@lmthing/config/tsconfig/node-lib",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"],
  "comment:exclude": "src/web/app.tsx and src/app/runtime/** are BROWSER entries (react-dom/client, DOM globals) bundled fresh by esbuild (web/serve.ts; app/build/pages.ts) — not part of this NodeNext Node program. Including them makes tsc deep-typecheck bundler-authored / DOM code under NodeNext and fail on extensionless imports + missing DOM lib.",
  "exclude": ["src/web/app.tsx", "src/app/runtime"]
}
