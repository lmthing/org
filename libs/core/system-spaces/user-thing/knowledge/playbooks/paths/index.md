One aspect per triage path that has a non-obvious call shape: how to run it, what to pass, what the
result looks like, and the mistakes that path has actually produced. Load the aspect named by the path
you picked, in the same statement you decide — paths 1 (answer directly) and 6 (memory) need no load.
