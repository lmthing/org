Everything about working inside a TEAM workspace rather than one person's — the directory, the
channels, the history reader and the writers, the conduct that a permanent shared log demands, and
the three team jobs that have their own tasklists. None of it applies on a personal workspace: no
`teamContext` in your ambient types means there is no team. The four always-on team rules are in
your instructions; load an aspect here the moment a request reaches past the thread you are in.
