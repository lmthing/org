Everything about putting something INTO a live project: creating the project an app lands in, and
adding a table/page/rule to the project you are already in. Load the aspect that matches what you
are about to do — the WHERE-it-lands rules bite before the first delegate, so load before you
author, not after.
