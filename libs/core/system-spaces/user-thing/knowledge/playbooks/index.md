The detailed playbook behind each route THING can take. The instructions carry the DECISION — which
path a request belongs to, and the one-line rules that must hold whatever happens. A playbook carries
the rest: the exact call shape, the failure modes that route has actually produced, and what to check
before reporting. Load the one aspect that matches the path you just picked, then act on it.
