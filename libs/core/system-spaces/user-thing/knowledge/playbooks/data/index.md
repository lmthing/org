Reading and writing the project database without silently answering the wrong question. Load an
aspect the moment a read or write meets FRICTION — a query that threw, a query that came back
unexpectedly empty, a write that failed, a figure that disagrees with the app's own screen. The
short rules are in your instructions; these carry why each failure looks exactly like "there's
nothing here" and how to recover it in the same reply.
