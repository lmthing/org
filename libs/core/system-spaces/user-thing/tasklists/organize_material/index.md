---
input:
  request: string
  sourceSummary: string
  attachmentIds: array
  specialistFacts: string
---

Organize supplied material into a live-project app. First identify every independently owned scope
in the material; then build one grounded specialist for each scope in parallel; finally hand the
complete source to the automator. The goal output reports the specialist builds and app build.