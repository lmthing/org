---
input:
  query: string
---

Produce a structured, cited research report for `query`: survey the topic broadly first, plan the
work into independent sub-questions grounded in that survey, investigate each one in parallel
against live web search and fetched pages, cluster the findings into themes, then summarize them
into one coherent, honest report. Ground everything in sources actually read — surface gaps and
uncertainty rather than hiding them.
