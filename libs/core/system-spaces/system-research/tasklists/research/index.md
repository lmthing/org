---
input:
  query: string
---

Answer `query` quickly with a shallow web search: one search, a concise grounded answer, and a
few sources. For deep multi-angle investigation use the `deep_research` tasklist instead.
