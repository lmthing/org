---
variable: viewBuildingAuthoring
description: >-
  The detail behind authoring freeform into a live project — the spec vocabulary a view is built
  from, what to do with a writer's rejection, and how data gets into the app. A FIRST whole-app
  build needs none of it; that goes to the build_live_project tasklist, which owns the whole
  pipeline and carries this detail in its own step prompts. Load the aspect that matches what you
  are about to write.
---

# Authoring freeform — the detail behind each job

Your instructions carry the DECISION (whether this is a first build or a change to an app that
already exists) and the rules that hold whatever you write. These aspects carry the rest: the
vocabulary, the recovery, and the shapes.
