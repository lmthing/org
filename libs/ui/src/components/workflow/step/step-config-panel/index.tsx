/**
 * StepConfigPanel — kept as a stub for backward compatibility.
 * Actual task editing is now done inline in TasklistEditor.
 * This component no longer renders anything meaningful.
 */
export function StepConfigPanel(_props: Record<string, unknown>) {
  return null
}
