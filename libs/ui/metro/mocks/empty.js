/**
 * The empty module RN's jest preset automocks `NativeExceptionsManager` to. Kept explicit (rather
 * than resolving to a real empty file somewhere in node_modules) so the substitution is greppable.
 */
module.exports = {}
