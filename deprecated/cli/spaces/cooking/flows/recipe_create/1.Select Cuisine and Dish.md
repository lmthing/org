---
description: "Determines the cuisine type and recommends a specific dish based on user preferences"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Based on the user's request and knowledge context (cuisine type, dietary restrictions, preferred cooking techniques), determine the appropriate cuisine and recommend a specific dish. Consider the occasion, number of servings, available time, and dietary needs. Load the relevant cuisine knowledge to inform the recommendation. Suggest a dish that matches the user's skill level and available equipment.

<output target="dishSelection">
{
  "type": "object",
  "properties": {
    "cuisineType": {
      "type": "string",
      "description": "Selected cuisine (e.g., 'Italian', 'Japanese', 'Mexican')"
    },
    "dishName": {
      "type": "string",
      "description": "Specific dish name"
    },
    "description": {
      "type": "string",
      "description": "Brief description of the dish and its origin"
    },
    "servings": {
      "type": "number",
      "description": "Number of servings"
    },
    "estimatedTime": {
      "type": "string",
      "description": "Total estimated cooking time (e.g., '45 minutes', '1h 30min')"
    },
    "difficulty": {
      "type": "string",
      "enum": ["beginner", "intermediate", "advanced"],
      "description": "Skill level required"
    },
    "rationale": {
      "type": "string",
      "description": "Why this dish fits the user's context and preferences"
    }
  },
  "required": ["cuisineType", "dishName", "description", "servings", "estimatedTime", "difficulty"]
}</output>
