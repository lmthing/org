---
description: "Calculates nutritional breakdown per serving using estimateCalories"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Analyze the nutritional content of the completed recipe. Use the estimateCalories function to calculate calorie totals from macronutrient estimates. Break down per-serving values for calories, protein, carbohydrates, fat, and fiber. Identify the primary nutrient sources from the ingredient list. Note any nutritional highlights or considerations relevant to the user's dietary restrictions.

<output target="nutritionAnalysis">
{
  "type": "object",
  "properties": {
    "perServing": {
      "type": "object",
      "properties": {
        "calories": { "type": "number" },
        "protein": { "type": "string", "description": "Grams of protein" },
        "carbohydrates": { "type": "string", "description": "Grams of carbs" },
        "fat": { "type": "string", "description": "Grams of fat" },
        "fiber": { "type": "string", "description": "Grams of fiber" }
      }
    },
    "nutrientSources": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "nutrient": { "type": "string" },
          "primarySource": { "type": "string", "description": "Main ingredient providing this nutrient" }
        }
      }
    },
    "dietaryNotes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Highlights or considerations for specific dietary needs"
    }
  },
  "required": ["perServing", "nutrientSources"]
}</output>
