---
description: "Generates preparation steps for all ingredients before cooking begins"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Create a detailed mise en place guide for the selected dish. Organize all preparation tasks that should be completed before cooking begins — washing, chopping, measuring, marinating, pre-heating. Group tasks by timing: what can be done hours ahead, what should be done right before cooking. Include knife cuts, sizes, and any technique notes for each preparation step.

<output target="miseEnPlace">
{
  "type": "object",
  "properties": {
    "advancePrep": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "task": { "type": "string" },
          "ingredient": { "type": "string" },
          "details": { "type": "string", "description": "Specific cut, size, or technique notes" },
          "timeAhead": { "type": "string", "description": "How far in advance this can be done" }
        }
      },
      "description": "Tasks that can be done hours or a day ahead"
    },
    "immediatePrep": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "task": { "type": "string" },
          "ingredient": { "type": "string" },
          "details": { "type": "string" }
        }
      },
      "description": "Tasks to do right before cooking"
    },
    "equipmentNeeded": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tools and equipment to have ready"
    }
  },
  "required": ["advancePrep", "immediatePrep", "equipmentNeeded"]
}</output>
