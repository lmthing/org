---
description: "Generates sequential cooking instructions with technique guidance and timing"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Generate the full cooking instructions for the dish. Load the relevant technique knowledge (sauté, grill, braise) to provide accurate method guidance. Each step should include precise timing, temperature, and sensory cues for doneness (visual, aroma, texture). Flag critical moments where timing or technique matters most. Include tips for common mistakes and how to recover from them.

<output target="cookingSteps">
{
  "type": "object",
  "properties": {
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "stepNumber": { "type": "number" },
          "action": { "type": "string", "description": "What to do" },
          "technique": { "type": "string", "description": "Cooking technique used (e.g., 'sauté', 'simmer', 'fold')" },
          "duration": { "type": "string", "description": "Time for this step" },
          "temperature": { "type": "string", "description": "Heat level or specific temperature" },
          "donenessCue": { "type": "string", "description": "How to know this step is complete (visual, aroma, texture)" },
          "tip": { "type": "string", "description": "Common mistake or pro tip for this step" }
        }
      }
    },
    "totalActiveTime": {
      "type": "string",
      "description": "Total hands-on cooking time"
    },
    "totalPassiveTime": {
      "type": "string",
      "description": "Total resting, simmering, or baking time"
    }
  },
  "required": ["steps", "totalActiveTime"]
}</output>
