---
description: "Generates a balanced weekly menu with meals assigned to each day"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.

<output target="weeklyMenu">
{
  "type": "object",
  "properties": {
    "days": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "day": { "type": "string" },
          "meals": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "mealType": { "type": "string", "description": "breakfast, lunch, dinner, or snack" },
                "dishName": { "type": "string" },
                "cuisineType": { "type": "string" },
                "cookingTime": { "type": "string" },
                "technique": { "type": "string", "description": "Primary cooking method" },
                "isLeftover": { "type": "boolean", "description": "Whether this uses leftovers from a previous meal" },
                "estimatedCalories": { "type": "number" }
              }
            }
          },
          "dailyCalories": { "type": "number", "description": "Total estimated calories for the day" }
        }
      }
    },
    "weeklyStrategy": {
      "type": "string",
      "description": "Overview of the meal plan strategy (variety, leftovers, prep batching)"
    }
  },
  "required": ["days", "weeklyStrategy"]
}</output>
