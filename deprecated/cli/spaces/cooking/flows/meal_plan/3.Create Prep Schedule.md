---
description: "Generates a batch prep schedule to minimize daily cooking effort"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.

<output target="prepSchedule">
{
  "type": "object",
  "properties": {
    "batchPrepSession": {
      "type": "object",
      "properties": {
        "day": { "type": "string", "description": "When to do batch prep (e.g., 'Sunday afternoon')" },
        "totalTime": { "type": "string" },
        "tasks": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "task": { "type": "string" },
              "usedFor": { "type": "array", "items": { "type": "string" }, "description": "Which meals this prep serves" },
              "duration": { "type": "string" },
              "storage": { "type": "string", "description": "How to store (fridge, freezer, container type)" },
              "shelfLife": { "type": "string", "description": "How long the prepped item keeps" }
            }
          }
        }
      }
    },
    "dailyPrepTasks": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "day": { "type": "string" },
          "tasks": {
            "type": "array",
            "items": { "type": "string" }
          },
          "activeTime": { "type": "string", "description": "Hands-on time needed" }
        }
      }
    }
  },
  "required": ["batchPrepSession", "dailyPrepTasks"]
}</output>
