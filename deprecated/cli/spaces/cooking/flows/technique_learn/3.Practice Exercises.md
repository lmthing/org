---
description: "Generates progressive practice exercises to build technique proficiency"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Design a set of progressive practice exercises that build the user's proficiency with this technique. Start with the simplest application and increase complexity. Each exercise should focus on one aspect of the technique — temperature control, timing, ingredient handling, etc. Include clear success criteria so the user knows when they've mastered each exercise. Use simple, inexpensive ingredients for practice.

<output target="practiceExercises">
{
  "type": "object",
  "properties": {
    "exercises": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "level": { "type": "number", "description": "Exercise number in progression" },
          "name": { "type": "string" },
          "focus": { "type": "string", "description": "Specific skill this exercise develops" },
          "ingredients": { "type": "array", "items": { "type": "string" } },
          "instructions": { "type": "string" },
          "duration": { "type": "string" },
          "successCriteria": { "type": "string", "description": "How to know you did it right" }
        }
      }
    },
    "progressionTips": {
      "type": "string",
      "description": "How to know when to move to the next exercise"
    }
  },
  "required": ["exercises", "progressionTips"]
}</output>
