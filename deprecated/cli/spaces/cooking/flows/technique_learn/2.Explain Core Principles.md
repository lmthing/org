---
description: "Teaches the science and principles behind the technique with visual cues"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Explain the core principles behind the cooking technique. Cover the science of what happens to food during this process — heat transfer, Maillard reaction, protein denaturation, etc. as relevant. Describe the sensory cues the user should watch for at each stage: visual changes, sounds, aromas, and textures. Include common mistakes and how to avoid them. Adjust the depth of explanation to the user's skill level.

<output target="corePrinciples">
{
  "type": "object",
  "properties": {
    "science": {
      "type": "string",
      "description": "The food science behind this technique (heat transfer, chemical reactions)"
    },
    "keyPrinciples": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "principle": { "type": "string", "description": "The rule or concept" },
          "why": { "type": "string", "description": "Why this matters" },
          "sensoryCue": { "type": "string", "description": "What to look, listen, or smell for" }
        }
      }
    },
    "commonMistakes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "mistake": { "type": "string" },
          "consequence": { "type": "string" },
          "fix": { "type": "string" }
        }
      }
    },
    "temperatureGuide": {
      "type": "string",
      "description": "Heat levels and temperature ranges for this technique"
    }
  },
  "required": ["science", "keyPrinciples", "commonMistakes"]
}</output>
