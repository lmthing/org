---
description: "Generates a consolidated grocery list organized by store section"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.

<output target="groceryList">
{
  "type": "object",
  "properties": {
    "sections": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "section": { "type": "string", "description": "Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')" },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "ingredient": { "type": "string" },
                "quantity": { "type": "string" },
                "unit": { "type": "string" },
                "usedIn": { "type": "array", "items": { "type": "string" }, "description": "Which meals use this ingredient" },
                "isSpecialty": { "type": "boolean", "description": "Whether this may need a specialty store" }
              }
            }
          }
        }
      }
    },
    "pantryCheck": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Items likely already in the pantry — verify before buying"
    },
    "estimatedTotal": {
      "type": "string",
      "description": "Rough cost estimate for the grocery list"
    }
  },
  "required": ["sections", "pantryCheck"]
}</output>
