---
description: "Generates a complete ingredient list with quantities, substitutions, and dietary alternatives"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Based on the selected dish, generate a complete ingredient list organized by category. Include precise quantities for the specified number of servings. For each ingredient that may conflict with dietary restrictions, provide a substitution. Flag any allergens. Use the scaleIngredients function if the user needs to adjust servings. Consider seasonal availability and suggest alternatives for hard-to-find items.

<output target="ingredientList">
{
  "type": "object",
  "properties": {
    "categories": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "Category name (e.g., 'Proteins', 'Vegetables', 'Pantry Staples')" },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "ingredient": { "type": "string" },
                "quantity": { "type": "string" },
                "unit": { "type": "string" },
                "substitution": { "type": "string", "description": "Alternative for dietary restrictions" },
                "allergens": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "Any allergens present (e.g., 'gluten', 'dairy', 'nuts')"
                }
              }
            }
          }
        }
      }
    },
    "servingsBase": {
      "type": "number",
      "description": "Number of servings this list is scaled for"
    },
    "pantryAssumptions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Common ingredients assumed to be on hand (salt, pepper, oil, etc.)"
    }
  },
  "required": ["categories", "servingsBase"]
}</output>
