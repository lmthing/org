---
description: "Determines the cooking technique to learn and assesses the user's current skill level"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Based on the user's request, identify the specific cooking technique they want to learn. Load the relevant technique knowledge (sauté, grill, braise) to understand the method in depth. Assess the user's current skill level and experience with related techniques. Determine what equipment is required and any prerequisites the user should know before starting.

<output target="techniqueProfile">
{
  "type": "object",
  "properties": {
    "technique": {
      "type": "string",
      "description": "Name of the cooking technique (e.g., 'Sautéing', 'Braising', 'Grilling')"
    },
    "category": {
      "type": "string",
      "enum": ["dry-heat", "moist-heat", "combination", "cold"],
      "description": "Heat transfer category"
    },
    "skillLevel": {
      "type": "string",
      "enum": ["beginner", "intermediate", "advanced"],
      "description": "Assessed skill level of the user"
    },
    "overview": {
      "type": "string",
      "description": "What this technique is and when to use it"
    },
    "requiredEquipment": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Essential tools and equipment"
    },
    "prerequisites": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Skills or knowledge the user should have first"
    }
  },
  "required": ["technique", "category", "skillLevel", "overview", "requiredEquipment"]
}</output>
