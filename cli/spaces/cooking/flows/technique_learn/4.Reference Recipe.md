---
description: "Generates a showcase recipe that demonstrates mastery of the learned technique"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Create a reference recipe that showcases the technique the user has been learning. This recipe should be a real dish from one of the user's preferred cuisines that relies heavily on this technique. Include detailed technique annotations throughout the cooking steps — marking where each principle from the lesson applies. Use the estimateCalories function for nutritional data. This recipe serves as both a culminating exercise and a keeper recipe for their collection.

<output target="referenceRecipe">
{
  "type": "object",
  "properties": {
    "dishName": { "type": "string" },
    "cuisineType": { "type": "string" },
    "servings": { "type": "number" },
    "totalTime": { "type": "string" },
    "ingredients": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "ingredient": { "type": "string" },
          "quantity": { "type": "string" },
          "unit": { "type": "string" }
        }
      }
    },
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "stepNumber": { "type": "number" },
          "action": { "type": "string" },
          "techniqueNote": { "type": "string", "description": "How this step applies the technique principles learned" },
          "duration": { "type": "string" },
          "donenessCue": { "type": "string" }
        }
      }
    },
    "nutritionPerServing": {
      "type": "object",
      "properties": {
        "calories": { "type": "number" },
        "protein": { "type": "string" },
        "carbs": { "type": "string" },
        "fat": { "type": "string" }
      }
    }
  },
  "required": ["dishName", "cuisineType", "servings", "ingredients", "steps"]
}</output>
