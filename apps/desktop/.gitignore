# Rust build output — large, machine-specific, and regenerated by every build.
src-tauri/target/
# Tauri's generated permission/capability schemas. Derived from capabilities/*.json at build time.
src-tauri/gen/
