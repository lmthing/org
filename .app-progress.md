# Project-as-Application — Implementation Progress

Tracking file per the implementation plan §0.4. One row per phase/sub-task, updated every step.

## Baseline reconciliation (pre-Phase-1) ✅ — sdk/org `6c08bc7`
- Fixed pre-existing red `pnpm typecheck`: excluded browser entry `libs/cli/src/web/app.tsx`
  from cli's NodeNext Node tsc program (bundled by esbuild at runtime, not a tsup entry).
- Quarantined 4 dead-fixture tests (keyless-cli, web-api) — `fixtures/` deleted in acb460a;
  filed `.issues/keyless-web-fixtures-removed.md` (also notes the serve-tree-ws teardown flake).
- Gate: build ✅ typecheck ✅ test ✅ (644 passed, 21 skipped).

## Wave A — Phase 1: Core db interfaces + capability globals + DTS refactor + projectRoot threading 🔴 LIVE

| Sub-task | Owner | Files | Status |
|---|---|---|---|
| 1A db interfaces + validator | subagent | `libs/core/src/db/**` | ✅ done — 12/12 tests, 0 db/ tsc errors. Exports: schema types (ColumnSchema/TableSchema/LoadedTable/RelationSchema…), api ifaces (DbApi/AsyncDbApi/ApiCallFn/SpawnFn), validateTableSchema/validateSchemaSet. AsyncDbApi = mapped type of DbApi. |
| 1B frontmatter/loader validation | subagent | `spaces/frontmatter.ts`, `spaces/load.ts`, new `spaces/capabilities.ts` | ✅ done — 70 spaces tests. Exports `AppCapabilities`, `CapabilityId`, `parseCapabilities(raw,{agentId,knownTables})`, `CAPABILITY_IDS`, `DB_CAPABILITY_IDS`. `AgentDef.capabilities?: AppCapabilities`, `LoadSpaceOpts.knownTables`, 9-key allow-list gate. |
| 1C DTS modularization | subagent | `typecheck/library-dts.ts` | ✅ done — 11 tests. Exports: EXEC_SHELL_DTS, WRITE_FILE_RAW_DTS, DB_{READ,WRITE,SCHEMA}_MEMBERS, composeDbDts({read,write,schema}), API_CALL_DTS, PAGES_WRITE_DTS, API_WRITE_DTS, HOOKS_WRITE_DTS, CAPABILITY_DTS_FRAGMENTS. LIBRARY_DTS re-appends write primitives (behavior-preserving for typecheckSource). |
| 1D+Integrator: projectRoot threading + registry + system-space audit + gate + live + push | orchestrator | `session/{types,session}.ts`, `fork/fork.ts`, `delegate/delegate.ts`, `globals/host-tools.ts`, `exec/{capability,bootstrap,fork-config,app-globals}.ts`, `system-spaces/*`, `index.ts` | ✅ done |

### Phase 1 — Core db interfaces + capability globals + DTS refactor + projectRoot threading  ✅
- Scope delivered:
  - `libs/core/src/db/` — `DbApi`(sync)/`AsyncDbApi`(Promise)/`ApiCallFn`/`SpawnFn` + schema types + fail-loud validator (1A).
  - `spaces/capabilities.ts` + `load.ts` — `capabilities:` frontmatter (config-bearing, fail-loud), 9-key allow-list gate, `AgentDef.capabilities` (1B).
  - `typecheck/library-dts.ts` — `execShell`/`writeFileRaw` split into `allowWrite`-gated fragments (bugfix), per-capability app fragments + `composeDbDts` + `CAPABILITY_DTS_FRAGMENTS` (1C).
  - Integrator: `CapabilityProfile.app` + `intersectAppCaps` (read-only roles drop write/authoring); `exec/app-globals.ts` (scope-checked `db` injection seam, gated on `projectRoot`); `buildAmbientDts` composes app DTS + gates write primitives; `projectRoot`/`projectId` threaded through session/fork/delegate + `LMTHING_PROJECT_DIR`/`LMTHING_PROJECT_ID` env; `index.ts` exports.
  - System-space audit: THING has NO `capabilities:` (correct — canDelegateTo lands P9); no read-only tasklist references the now-gated write primitives; all system spaces load under the new gate.
  - Updated 3 tests that encoded the old unconditional-write-primitive-DTS bug (bootstrap/roles/foreach) to the corrected behavior + new `capability-app.test.ts`.
- Gate: build ✅ typecheck ✅ (6/6) test ✅ (695 passed, 21 skipped; +51) system-spaces-dag ✅ lint:tokens n/a (no web styling).
- Non-breaking: full pre-existing suite re-run green; the only changed pre-existing tests were the 3 that asserted the fixed bug.
- 🔴 Live evidence (`--model S` DeepSeek):
  - `/tmp/p1-thing.ndjson` — THING smoke: loads, streams, `display()`, turn done (behavior preserved; node_start/end balanced).
  - `/tmp/p1-caps.ndjson` — capabilities fixture: space loaded; `db.query("items")` (granted db:read) PASSED typecheck (runtime `'db' is not defined` — no projectRoot ⇒ app globals not injected, invariant proven); `writePage(...)` (not granted) FAILED typecheck `Cannot find name 'writePage'` (stray non-listed global rejected).
- Follow-ups: value-yielding app globals (`apiCall`/`writePage`/`writeApi`/`writeHook`) are DTS-only in P1; their runtime injection lands in owning phases (P3 api runtime, P9 authoring) once yield resolvers exist. Engine impl for the `db` seam lands P2.

## Wave B — Phase 2: CLI db engine + project app loader 🔴 LIVE (in progress)

better-sqlite3@11.10.0 added to libs/cli + native build approved (root pnpm.onlyBuiltDependencies)
+ compiled locally (Node 24 ABI). **Devops note:** pod image/CI need prebuilt binaries or a build
toolchain (python3/make/g++) for better-sqlite3.

| Sub-task | Owner | Files | Status |
|---|---|---|---|
| 2A store engine (better-sqlite3 → DbApi/AsyncDbApi, dump/restore, migrations) | subagent | `libs/cli/src/app/store.ts` (+ test) | ⏳ |
| 2B project app loader + boot reconcile | subagent | `libs/cli/src/app/{loader,boot}.ts` (+ tests) | ⏳ |
| 2C backup edits (exclude app.db, pre-dump app.sql) | subagent | `server/backup.ts` (+ test) | ⏳ |
| Integrator: session-manager appGlobals/projectRoot seam (+ core SessionOpts.appGlobals) + bin.ts headless wiring + live + push | orchestrator | `session/{types,session}.ts`, `cli/bin.ts`, `server/session-manager.ts` | ✅ done |

### Phase 2 — CLI db engine + project app loader  ✅
- Scope delivered:
  - `libs/cli/src/app/store.ts` (2A) — better-sqlite3 `ProjectDb`: sync `DbApi` + async mirror, schema→CREATE TABLE (FK onDelete cascade/setNull/restrict), value marshalling (bool/json/date), relation `include` join, `.sql` dump/restore, `schemaToCreateTableSql`. 21 tests.
  - `libs/cli/src/app/{loader,boot}.ts` (2B) — `loadProjectApp` (validate `database/*.json`, tolerate spaces-only `system`), `bootProjectApp` (DR-restore-if-absent → open → additive reconcile, fail-loud non-additive). 13 tests.
  - `server/backup.ts` (2C) — exclude `**/.data/app.db(-*)`; `dumpAllProjectDbs(root)` regenerates each `.data/app.sql` before git staging. 4 tests.
  - Integrator: core `SessionOpts.appGlobals` threaded into the session VM + forks + delegates; `bin.ts` headless `--request` boots the `user` project db + passes `projectRoot`/`appGlobals`; `SessionManager` lazily boots + caches a per-project `ProjectDb` (`getProjectAppGlobals`, `closeProjectDbs`) and wires the fresh + resume session paths.
  - better-sqlite3@11.10.0 added to `libs/cli`; root `pnpm.onlyBuiltDependencies` approves its native build.
- Gate: build ✅ typecheck ✅ test ✅ (733 passed, +38; the lone red is the known `serve-tree-ws` parallel-teardown flake — passes in isolation, pre-existing, documented in `.issues/`).
- 🔴 Live (DeepSeek `--model S`, fresh root):
  - `/tmp/p2-caps.ndjson` — `db:write` agent `db.insert("feed_items",…)` → row durably in `app.db` (uuid + `createdAt` generated, `read` default). Verified via direct sqlite read.
  - `/tmp/p2-scope.ndjson` — `db:write:{tables:[notes]}` agent blocked writing `feed_items`: `db db:write: table "feed_items" not permitted; allowed tables: notes`; and its `db.query` fails typecheck (per-verb gating).
- Follow-ups: value-yielding app globals still DTS-only (P3/P9); `SessionManager.closeProjectDbs()` not yet called on server shutdown (handles close on process exit; low priority); a serve-startup pre-boot loop (reconcile all projects eagerly) deferred — boot is currently lazy per first session. **Devops:** pod image/CI need better-sqlite3 prebuilt binaries or a build toolchain.

Shared `ProjectDb` factory (2A owns in `app/store.ts`; 2B/2C import): `openProjectDb(dbPath, {create?}): ProjectDb`
where `ProjectDb = { db: DbApi; async: AsyncDbApi; raw: Database; dumpToSql(): string; listTables(): string[]; tableColumns(t): string[]; close(): void }`; plus `restoreFromSql(dbPath, sql)`.

## Wave C — Phase 3: API runtime (Node worker-isolated file-based handlers) 🟢 (in progress)

| Sub-task | Owner | Files | Status |
|---|---|---|---|
| 3A api runtime core (file router, worker isolation, ctx db/spawn/apiCall proxies, HttpError, spawn) | subagent | `libs/cli/src/app/api/**` | ⏳ |
| 3B method-aware input assembly + error contract seam | subagent | `libs/cli/src/app/api/{input,errors}.ts` | ⏳ |
| Integrator: mount `/app/:projectId/api/*` in serve.ts (below reserved /api/*) + wire main-process db/spawn seam + curl integration + push | orchestrator | `server/serve.ts`, `server/routes/app-api.ts`, `server/session-manager.ts`, `tsup.config.ts` | ✅ done |

### Phase 3 — API runtime (Node worker-isolated file-based handlers)  ✅
- Scope delivered:
  - `libs/cli/src/app/api/` (3A) — `loader.ts` (endpoint=dir, method=filename; route table by pattern + by `name`, fail-loud dup-name), `worker.ts` + `handler-module.ts` + `protocol.ts` (worker-thread crash boundary; `ctx.db`/`spawn`/`apiCall` = async message-channel proxies to main — no Atomics), `runtime.ts` (`createApiRuntime`, `handle(method,path,input)`, `callByName`; esbuild-transpile handler + run in worker; crash → generic 500 logged-not-leaked). 14 tests.
  - `errors.ts`/`input.ts` (3B) — `HttpError` + error contract (+ cross-thread serialize/reconstruct), method-aware `assembleInput` (path∪query GET/DELETE, path∪body POST/…), `passThroughValidator` seam (ajv in P4). 25 tests.
  - Integrator: `server/routes/app-api.ts` mounts `/app/<project>/api/*` (below reserved `/api/*`) with a per-project `ApiRuntime` cache; `SessionManager.getProjectDb` exposes the booted `ProjectDb.async` to the runtime; `spawnRunner` is a Phase-3 seam (runId placeholder; real runner = P6); `tsup.config.ts` emits `dist/worker.js` (self-contained sibling of the bundled runtime — required because tsup flattens src into single dist entries).
- Gate: build ✅ typecheck ✅ test ✅ (772 passed, +39; lone red = known `serve-tree-ws` teardown flake, passes isolated).
- 🟢 Integration (curl against `lmthing serve`, no LLM): `GET /app/user/api/feed-list` → row via worker→main db proxy (marshalled `read:false`/ISO date); `POST /app/user/api/mark-read {id}` → `{ok:true}` and the db `read` flag flipped to 1 (durable); `HttpError(400)` → 400; unknown endpoint → 404. Full path: HTTP→mount→runtime→worker isolation→async db proxy→real db read/write→error contract.
- Follow-ups: `spawn` runner + `apiCall` agent-side wiring land with P6/P4; ajv validation replaces `passThroughValidator` in P4.

## Wave C — Phase 4: Typed-contract build pipeline 🔴 LIVE (in progress)

Deps added to libs/cli: `ajv@^8.17.1`, `ts-json-schema-generator@^2.3.0` (pure-JS).

| Sub-task | Owner | Files | Status |
|---|---|---|---|
| 4A schema + row-type generation → types/generated.d.ts + EndpointContract[] | subagent | `libs/cli/src/app/build/schema.ts` | ⏳ |
| 4B four consumers: ajv validator (replaces passThroughValidator), apiCall DTS overloads, tool signatures | subagent | `libs/cli/src/app/build/{validate,apicall-dts}.ts` | ⏳ |
| Integrator: wire generation + ajv into api runtime + thread apiCall overload DTS into session (core SessionOpts.appDts) + live + push | orchestrator | `app/build/contracts.ts`, `app/api/runtime.ts`, `server/{session-manager,routes/app-api}.ts`, `cli/bin.ts`, core `session/{types,session}.ts` + `exec/bootstrap.ts` | ✅ done |

### Phase 4 — Typed-contract build pipeline  ✅
- Scope delivered — all 4 consumers of TS+JSDoc → JSON Schema:
  - `app/build/schema.ts` (4A) — row types from `database/*.json` (typed relation fields, JSDoc from descriptions) + `ts-json-schema-generator` for endpoint `Input`/`Output` → JSON Schema + compact TS-type strings; writes `<project>/types/generated.d.ts`. 9 tests.
  - `app/build/{validate,apicall-dts}.ts` (4B) — ajv `makeInputValidator`/`makeValidatorMap` (coerceTypes), `buildApiCallDts` (typed overloads), `buildApiToolSignatures`. 12 tests. (ajv v8 `Ajv.default` interop handled.)
  - Integrator: `app/build/contracts.ts` bundles generation; `apiCall` DTS is emitted **strict (no generic fallback)** for the agent so a wrong name OR wrong input type fails typecheck (added `buildApiCallDts(…, {fallback:false})`); ajv validators injected into the api runtime (`ApiRuntimeOpts.validators`, replacing the passThrough seam); core `SessionOpts.appDts` + `buildAmbientDts` use project overloads for `api:call`; `SessionManager.getProjectContracts` caches per project; wired into headless (`bin.ts`) + server (`session-manager`/`app-api`).
- Gate: build ✅ typecheck ✅ test ✅ (793 passed, +21).
- 🔴 Live + 🟢 integration evidence:
  - `/tmp/p4-apicall.ndjson` — `apiCall('markRead', { id: 123 })` → **"No overload matches this call… Type 'number' is not assignable to type 'string'"** (typed DTS gate, consumer 3).
  - HTTP: `POST mark-read {}` → `400 {"error":{... "invalid input", details:[missingProperty:"id"]}}` (ajv, consumer 2); valid id → `{ok:true}`.
  - `types/generated.d.ts`: `FeedItem` row + `MarkReadInput {id:string}` / `FeedListOutput {items:unknown[]}` (consumers 1+4).
- Follow-ups: agent-side **runtime** `apiCall` invocation (value-yielding global + yield-router resolver + allow-list host error) is DTS-only per the P1 design — wire in its owning phase (pairs with P6/P9). Inline handler return types (not `export interface Output`) generate `Output {}` — demo/docs handlers should declare `Output`.

Shared `EndpointContract` (4A owns; 4B + integrator consume): `{ name: string; method: HttpMethod; routePath: string; description: string; inputSchema: object; outputSchema: object; inputTsType: string; outputTsType: string }`.

### Shared contracts (integrator wires these together)
- `AppCapabilities` type + `parseCapabilities`/`validateCapabilities` — owned by **1B** in `spaces/capabilities.ts`.
  Cap ids: `db:read`/`db:write`/`db:schema` (config `{tables?: string[]}`), `pages:write`/`api:write`/`hooks:write` (bare only),
  `api:call` (config `{allow: string[]}`, REQUIRED — bare throws). Frontmatter allow-list =
  `{title,knowledge,functions,components,actions,defaultAction,canDelegateTo,dependencies,capabilities}`.
- `DbApi` (sync agent surface) / `AsyncDbApi` (Promise, Node) / `ApiCallFn` / `SpawnFn` + schema types + validator — owned by **1A** in `db/`.
- `CAPABILITY_DTS_FRAGMENTS` map (capability-id → ambient declare string) + gated `EXEC_SHELL_DTS`/`WRITE_FILE_RAW_DTS` — owned by **1C** in `typecheck/library-dts.ts`.
