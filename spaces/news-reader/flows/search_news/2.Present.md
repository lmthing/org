---
step: 2
name: Present
agent: curator
tasks: [present]
---

# Phase 2 — Present results

```ts
__flow.start("present");
const articles = /* from validate */;
const query = /* original query */;

const summary = articles.length === 0
  ? `No results found for "${query}". Try broadening your search or changing the freshness filter.`
  : `Found ${articles.length} articles for "${query}". Sources: ${[...new Set(articles.map(a => a.source))].join(", ")}.`;

submitResults({ query, articles, summary });
__flow.finish("present");
```
